# Optional. If unset during `npm run dev`, the app uses /api (Vite proxies to http://localhost:8080).
# Use a full URL when the API is not on localhost:8080, or when you run `vite build` + static hosting.
#
# VITE_API_BASE_URL=http://localhost:8080/api
